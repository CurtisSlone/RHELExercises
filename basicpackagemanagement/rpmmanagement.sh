# Install a package using rpm (without automatic dependency resolution)
sudo rpm -i package_name.rpm

# Upgrade a package using rpm
sudo rpm -U package_name.rpm

# Remove a package using rpm
sudo rpm -e package_name

